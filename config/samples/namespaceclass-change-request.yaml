apiVersion: akuity.io/v1alpha1
kind: NamespaceClassChangeRequest
metadata:
  name: public-network-approval
spec:
  namespaceClassName: public-network
  recommendationID: rec-xxx
  approved: true
  allowHighRisk: false
  # Required when NamespaceClass has namespaceclass.akuity.io/require-pull-request: "true"
  # or when the controller sets NAMESPACECLASS_REQUIRE_PULL_REQUEST_URL
  # pullRequestURL: https://github.com/org/repo/pull/42
